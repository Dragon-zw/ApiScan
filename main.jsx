import React from 'react';
import ReactDOM from 'react-dom/client';
import APIPentestPlatform from './api_pentest_platform.tsx';
import './index.css';

// 应用入口文件
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <APIPentestPlatform />
  </React.StrictMode>
);
